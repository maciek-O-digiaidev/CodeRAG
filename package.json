{
  "name": "coderag",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20",
    "pnpm": ">=8"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "lint": "eslint .",
    "clean": "pnpm -r run clean"
  },
  "devDependencies": {
    "eslint": "^9.20.0",
    "typescript-eslint": "^8.24.0",
    "typescript": "^5.7.3",
    "prettier": "^3.5.1"
  }
}
